# RELAY — 중계

브라우저에서 바로 돌아가는 뱀서라이크 로그라이크. 빌드 도구 없이 **단일 HTML 파일** 하나로 동작합니다.

**플레이:** https://relay-game-leedongwooks-projects.vercel.app

---

## 이 게임의 차별점

### 신호권 (Signal Zone)

화면 어딘가에 황금빛 원이 있습니다. 그 안에서 싸우면 **피해 170%**, 밖에서는 100%. 구역은 32초마다 다른 곳으로 옮겨 가고, 적은 플레이어가 아니라 **구역 쪽으로** 몰려옵니다. 가장 안전한 곳이 가장 붐비는 곳이 되는 구조라, 매 32초마다 "버틸지 뚫고 갈지"를 결정해야 합니다.

구역 밖에 30초 이상 머무르면 간섭이 발생해 화면에 노이즈가 끼고 적이 빨라집니다.

### 2단계 오퍼레이터 진화

8레벨에 갈래를 고르고, 16레벨에 최종 형태가 됩니다. 갈래에 따라 기체 모양, 전용 무기, 궁극기, 플레이 방식이 전부 달라집니다.

| | 방랑자 → 유령 | 수호자 → 요새 |
|---|---|---|
| 철학 | 신호권에 안 매임 | 신호권을 지킴 |
| 피해 | 어디서든 135% | 안 220% / 밖 85% |
| 1차 능력 | 이동 중 25% 회피 | 6초마다 피격 1회 무효 |
| 2차 능력 | 잔상 분신 2체가 함께 사격 | 자동 포탑 2기 궤도 사격 |
| 전용 무기 | 유도 미사일 | 충격 망치 |
| 궁극기 | 잔상 질주 (3초 무적·이속 2배) | 역송신 (신호권을 내 위치로) |

진화를 거부하는 세 번째 선택지도 있습니다. 레벨업 카드가 4장이 되고 피해 +15%.

### 무기 진화

무기 Lv4 + 특정 패시브 Lv3 → 진화. 레일건, 검풍 폭풍, 뇌우, 지옥불, 궤도 폭격, 지진.

레벨업 카드에 진화 조건과 달성도가 표시되므로 위키를 뒤질 필요가 없습니다.

### 데일리 시드

KST 날짜로 시드를 생성해, 같은 날 접속한 모두가 **같은 신호권 경로 · 같은 적 구성 · 같은 카드 순서**로 플레이합니다. 운이 아니라 실력으로 순위가 갈리도록.

---

## 조작

- **이동** WASD · 방향키 · 화면 드래그 (모바일)
- **공격** 자동
- **궁극기** Space 또는 게이지 탭
- **레벨업** 숫자 키 선택 · `R` 리롤(2회) · `S` 스킵
- **기타** `Esc` 일시정지 · `M` 음소거

---

## 기술 구조

의존성 없음. 프레임워크 없음. 빌드 단계 없음.

| 영역 | 구현 |
|---|---|
| 렌더링 | Canvas 2D, `shadowBlur` 글로우, 패럴랙스 별 배경 |
| 사운드 | Web Audio API로 실시간 합성 (오디오 파일 0개) |
| 저장 | `localStorage` (프로필 · 개인 기록) |
| 랭킹 | Supabase REST + PostgreSQL 함수 |
| 배포 | Vercel 정적 호스팅 |

### 랭킹 백엔드

`supabase.sql`을 Supabase SQL Editor에 붙여 넣으면 테이블·검증 함수·뷰·RLS가 한 번에 생성됩니다.

점수는 클라이언트가 아니라 **PostgreSQL 함수가 다시 계산**합니다. 생존 시간 대비 불가능한 처치 수나 레벨은 거부되고, RLS로 테이블 직접 쓰기는 막혀 있어 함수를 통해서만 기록이 들어갑니다. 하루 한 기기당 한 줄이 유지되며 최고점만 갱신됩니다.

```
점수 = 처치×10 + 생존초×4 + 레벨×80 + 클리어 4000
```

### 직접 돌리기

```bash
git clone https://github.com/inviy/relay-game.git
cd relay-game
python3 -m http.server 8000   # 또는 그냥 index.html을 브라우저로 열기
```

랭킹 없이 플레이하려면 `index.html` 상단의 `SB_KEY`를 비워두면 됩니다. 게임은 정상 동작하고 랭킹 영역만 비활성화됩니다.

`SB_KEY`는 Supabase의 anon public 키로, 공개되어도 안전하도록 설계된 값입니다. 실제 권한은 RLS 정책이 통제합니다.

---

## 앞으로

- 시작 오퍼레이터 선택 (첫 60초의 단조로움 해소)
- 도감 — 발견한 진화 레시피와 적 정보 기록
- 목표형 해금 (특정 조건 달성 시 새 시작 옵션)
- 사망 화면 공유 이미지
- 적 종류 확장과 타임라인 이벤트 추가

---

## 에셋

기체·적 스프라이트는 [Kenney — Pixel Shmup](https://kenney.nl/assets/pixel-shmup) (CC0 1.0)을
사용했습니다. 32×32 스프라이트시트를 base64로 인라인해 외부 요청 없이 단일 HTML을 유지하며,
회색조 스프라이트에 게임 팔레트를 틴트해 씁니다. CC0라 출처 표기 의무는 없지만 밝혀 둡니다.

## 라이선스

MIT (코드) · 스프라이트는 CC0
